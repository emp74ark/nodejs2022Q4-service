import { PublicFieldsInterceptor } from './public-fields.interceptor';

describe('PublicFieldsInterceptor', () => {
  it('should be defined', () => {
    expect(new PublicFieldsInterceptor()).toBeDefined();
  });
});
